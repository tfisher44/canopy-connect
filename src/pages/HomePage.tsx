export function HomePage() {
  return (
    <section className="panel stack">
      <h2>Home</h2>
      <p>
        This starter uses React + TypeScript + Zod + React Hook Form + Radix UI with a placeholder
        ArcGIS integration surface.
      </p>
      <p className="muted">
        Next phase: replace Map placeholder with editable ArcGIS map and table write flow.
      </p>
    </section>
  );
}
